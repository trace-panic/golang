## Go Basic User Server
- Built with `julienschmidt/httprouter` library
- Using MongoDB database and `gopkg.in/mgo.v2` for ORM, more of just a DB layer

How to run it.

```bash
go build
go run main.go
```
